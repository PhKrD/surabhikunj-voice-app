<?xml version="1.0" encoding="utf-8"?>
<!--
  Config for VoiceKidsAccessibilityService.

  packageNames restricts which apps' events even reach this service — kept
  narrow to known browsers only, both for privacy (we never want events
  from arbitrary apps) and performance (fewer events to debounce/process).
  Must stay in sync with VoiceKidsAccessibilityService.BROWSER_URL_BAR_IDS.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeViewTextChanged|typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagReportViewIds"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="500"
    android:packageNames="@array/monitored_browser_packages"
    android:description="@string/accessibility_service_description" />
